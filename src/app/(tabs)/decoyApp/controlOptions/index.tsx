import ControlOptions from '@/components/screens/decoyApp/controlOptions';

// need to check again because if [id] param
export default function ControlOptionsScreen() {
  return <ControlOptions />;
}
